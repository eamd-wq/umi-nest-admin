const fetchUserInfo = async () => {
  return void 0;
};

const initialState: InitialStateType = {
  name: 'Umi Nest Admin',
  isLogin: false,
  access: [],
  userInfo: {},
  theme: 'light',
  fetchUserInfo,
};

export default initialState;
